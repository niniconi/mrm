<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.niniconi.mrm.mapper.resource.ResTypeMapper">
    <insert id="add">
        INSERT INTO res_types(types) VALUES(#{type});
    </insert>
    <insert id="del">
        DELETE FROM res_types WHERE types=#{type};
    </insert>
</mapper>